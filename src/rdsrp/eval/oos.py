"""Orchestrates OOS training/prediction and stores predictions per month."""
from __future__ import annotations

import pandas as pd


def run_oos_forecast(panel: pd.DataFrame) -> pd.DataFrame:
    """Return DataFrame with predictions per model, per stock-month."""
    # Stub
    return pd.DataFrame()
